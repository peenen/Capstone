def handle_missing_values(df, method='mean'):
    # Fill missing values: 'mean', 'median', 'zero'
    return df

def handle_duplicate_values(df):
    # Remove duplicate rows
    return df

def detect_outliers(df, method='zscore'):
    # Detect outliers via IQR or Z-score
    return df
